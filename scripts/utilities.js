//function forEach = function(array, callback) {
//    for (var 1 = 0; i < array.length; i++)
//        callback(array[i]);
//};

function forEach(array, callback) {
  for (var i = 0; i < array.length; i++) {
    callback(array[i]);
  }
};